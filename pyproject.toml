[project]
name = "sample-graphrag"
version = "0.1.0"
description = "Standalone GraphRAG demo using neo4j-graphrag and OpenAI"
requires-python = ">=3.12"
dependencies = [
    "neo4j-graphrag[openai]>=1.14.0",
    "python-dotenv>=1.0.0",
]
