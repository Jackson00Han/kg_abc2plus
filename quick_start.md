# 快速启动与重启知序

日常使用入口：<http://127.0.0.1:8002/industrial>。

## 最简单的方法（当前 Mac）

在 Finder 中打开本项目，双击 **`quick_start.command`**。

也可以在项目目录的终端运行：

```sh
./quick_start.command
```

服务没启动时，该命令会启动服务；已经启动时，会重启本项目的 8002 服务。
看到“服务已启动”后刷新网页。关闭启动用的终端，服务仍会在后台运行。

如果系统不允许双击，直接在终端运行上面的命令即可。也可从任意目录运行：

```sh
"/Users/shanzhonghan/Documents/mini_projects/ai_power/ai_power_kg/kg_training/sample-graphrag/quick_start.command"
```

## 重启会保留什么

- 复用同一个 Neo4j 数据库，保留本体、资料、审核与发布记录，不执行重置或示例数据导入。
- 始终开启当前“仅循环水泵测试包”隔离模式，不启用旧 8000 页面或隔离资料。
- 跳过外部模型预热，因此启动不调用大模型；实际上传与检索仍需要配置的模型服务。
- 旧页面身份可能因服务重启失效，刷新后重新进入当前知识库即可。未提交的页面编辑建议先保存。

脚本会等待原服务正常退出，不强制杀死正在收尾的任务；只有确认端口上的进程属于
当前项目才会停止它。如果已有重启操作正在进行，第二次运行会直接提示。

## 配置与日志

当前机器的配置已准备好，不需要手工填写密码：

| 文件 | 用途 |
| --- | --- |
| `.env` | 模型地址、模型名称与 API Key |
| `.env.workbench.local` | 当前数据库连接、原 Docker 容器名称；仅当前用户可读写，已被 Git 忽略 |
| `.local/workbench/8002.log` | 服务日志，已被 Git 忽略 |
| `.local/workbench/8002.pid` | 最近启动的服务 PID，仅供查看 |
| `scripts/restart_workbench.py` | 重启实现 |

查看最近日志：

```sh
tail -n 60 .local/workbench/8002.log
```

不要将 `.env` 或 `.env.workbench.local` 提交到 Git、截图或公开分享。

## 遇到问题

- **Docker 未启动**：先打开 Docker Desktop，等它就绪后再次运行。脚本可启动已有且停止的数据库容器，但不会创建替代数据库或删除容器。
- **8002 被其他程序占用**：脚本会停止操作，避免误关其他服务；先确认占用程序。
- **服务仍在收尾**：等待当前任务结束，再次运行；脚本不会强制终止任务。
- **缺少 `.env.workbench.local`**：通常发生在新电脑或重新克隆仓库。需先恢复同一个数据库及其连接配置，不能仅凭源码恢复知识数据。该文件需要 `PLAYGROUND_NEO4J_URI`、`PLAYGROUND_NEO4J_USER`、`PLAYGROUND_NEO4J_PASSWORD`、`PLAYGROUND_NEO4J_DATABASE=neo4j`，以及可选的 `WORKBENCH_NEO4J_CONTAINER`。请使用真实的现有数据库配置，不要复制别人的密码。
- **缺少 `.venv`**：在项目目录按项目依赖说明准备 Python 3.12 环境与依赖，再运行脚本。快速启动不自动重建环境或安装软件。

此入口针对当前 Mac 和已有水泵测试库；它不是新电脑的一键初始化或生产部署脚本。

## 本次验证

2026-09-10 已实际通过此入口重启 8002，页面可访问，水泵库保留 4 个节点、3 条关系、
4 条属性；隔离模式保持开启。3 项进程识别与数据库预检保护测试通过，Shell 语法及
差异检查通过。未通过停止数据库容器或故意中断模型任务进行测试。
