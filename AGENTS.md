# 项目开发约定

本文件只记录长期适用的开发规则。具体任务以用户当前要求为准；历史验证报告和实现说明查阅 `docs/`，验收指标查阅 `contracts/`。历史阶段完成不代表后续修改已通过同等验证，也不等于已部署生产环境。

## 工作范围与代码组织

- 围绕当前 Neo4j GraphRAG 应用开发，沿用现有模块边界；领域模型、摄取、图谱治理、检索、生成、API 和评估职责保持分离。
- 修改前检查工作区状态和相关实现，保护用户已有改动，不顺手重构或修改无关文件。
- 提交、推送和部署按用户授权执行，不因历史计划自动推送到 `main`；不重写已发布的 Git 历史。
- 不提交 `.env`、凭据、生成缓存、本地数据库或虚拟环境；日志默认不记录受保护的原文和秘密。

## 产品与设计判断

- 用户提出的设计想法是需要认真评估的输入，不应机械照搬。先理解其目标，结合信息层级、视觉秩序、可发现性、可读性和操作效率独立判断；明确的要求与约束仍须遵守。存在更好的方案时，说明取舍并在授权范围内直接落实。
- 页面追求干净、有序、简单、美观，但不能以隐藏必要状态、丢失关键操作或增加理解成本换取表面简洁。低频信息按需展示，核心任务与证据入口保持清晰。
- 当前产品只设计与验收电脑端，覆盖适用的笔记本、桌面宽屏和全屏状态，不主动新增手机端适配。前端布局或交互调整须连接真实浏览器进行视觉验收，查看实际渲染与关键操作。代码、DOM 和 HTTP 检查不能替代视觉验收；连接失败时先排查并尝试恢复，未完成的验收必须明确记录，不得宣称已经通过。

## 浏览器验收与故障恢复

- CUA 的浏览器列表为空或返回 `Browser is not available`，只说明该连接不可用。首次连接失败后，检查项目已有浏览器脚本和技术验收记录，直接尝试 **Playwright 驱动的独立 Chromium**；用户已授权此备用方案，不要重复请求许可，也不要反复尝试同一失败连接后就宣布无法验收。
- 固定入口：`sh scripts/run_browser_qa.sh --check`。脚本检查并修复固定版本的 Playwright 依赖，实际启动 Chromium，必要时调用官方浏览器安装器再试一次，然后打开当前 `http://127.0.0.1:8002/industrial` 并截图。工具默认安装在 `${XDG_DATA_HOME:-$HOME/.local/share}/graphrag-browser-qa/` 下的版本目录；不要把 `/tmp` 中的旧依赖当成长期可用环境，也不要为浏览器验收修改应用依赖。
- 存在至少两个可访问发布版本时，用 `sh scripts/run_browser_qa.sh --check --publication` 验证历史选择、真实比较接口、弹窗和关闭操作；该检查拦截知识写入，不执行版本切换。其他页面复用 `sh scripts/run_browser_qa.sh verify_<task>.cjs`；先核对目标脚本的数据范围、请求白名单和输出路径。
- 分开诊断模块缺失、浏览器二进制缺失/损坏、应用服务不可达。前两类检查入口脚本的安装和启动日志；若官方安装后仍无法启动，按具体错误修复运行依赖或使用官方 `install --force chromium` 修复缓存，再实际启动验证。服务故障按现有 8002 启动流程恢复，并核对 bootstrap 为 `pump-only`；不得启动旧 8000、重置用户数据库或引入隔离语料来绕过故障。
- 使用独立临时浏览器上下文，不接管或关闭用户日常 Chrome，不使用其个人配置。默认只读验收当前知识；需要发布、切换、删除等写入的测试使用隔离夹具，或在用户明确授权的操作范围内执行。
- 连接检查输出在 `.local/browser-qa/recovery/`，可用 `BROWSER_QA_OUTPUT` 指定目录。必须实际查看截图，并验证本次改动的关键操作、滚动及适用的全屏状态；至少覆盖 1280、1440、1920 三种桌面宽度。连接检查通过不等于所有业务交互验收通过。只有 CUA 和独立 Chromium 都经过实际排查仍不可用，才能报告浏览器阻塞，并记录具体错误和尝试过的恢复命令。技术依据和恢复实测见 `docs/validation/browser-recovery-2026-09-11.md`。

## 数据、身份与证据

必须保持以下链路可追溯：

> 可信来源 → 不可变文档版本 → 精确 Chunk 位置 → 受治理的图谱 → 有界检索 → 带引用的回答 → 可复现验证

- 文档、Chunk、实体和关系使用稳定的应用 ID，不将 Neo4j 内部 `elementId` 用作持久业务标识。
- 保留来源、文档版本、校验和、原文位置、抽取或人工形成记录及审核历史。数据修订不得覆盖历史证据。
- 实体身份不能仅凭同名判定；遵守现有本体约束，身份关联和合并必须有可审计依据。
- 图实体和关系用于导航，回答的事实依据仍是来源 Chunk；缺乏证据的抽取结果不得直接作为已确认事实发布。
- 来源等级、形成方式和审核状态分别保留；人工确认不得自动提升来源等级。
- 摄取和重试保持幂等；更新、删除、撤回及恢复保持版本、证据和发布状态一致，避免误删其他来源仍支持的数据。
- 嵌入模型、索引和配置版本必须明确，不能静默混用不同向量空间。

## 权限、检索与回答

- 在召回、图扩展、相邻 Chunk 补全、知识浏览和证据读取的查询中执行租户与访问过滤，不能只在最终结果中补做过滤。
- 聚合、计数和来源展示同样遵守权限；候选、已发布和历史版本的边界必须明确。
- 查询、图扩展、上下文、并发、超时和重试均须有界；达到上限或依赖失败时明确报告，不能将部分结果冒充完整结果。
- 优先采用成熟且有文档的检索与图算法；自定义评分公式须经用户明确同意并完成对比评估。
- 回答中的关键事实附 Chunk 级引用，能够定位到文档版本和原文范围；保留数字、日期、货币和单位，明确区分原文事实与推断。
- 证据不足时说明无法回答；来源冲突时保留差异及适用时间，不静默覆盖或编造结论。

## 开发与验证

- 本地开发默认使用版本化的 `dev-mini` 配置，只降低规模、持续时间和重复次数，不削弱身份、溯源、权限、正确性或生命周期约束。开发规模结果不能冒充生产候选验证。
- 保留分层数据集：快速单元测试使用小型夹具，代表性多文档验证使用 `dev-corpus-v1`，容量与持续负载验证使用对应的生产参考配置。
- 根据改动风险运行必要的自动化检查。行为修改补充相关测试；涉及 Neo4j 查询、事务或持久化时运行对应的真实 Neo4j 集成测试；权限修改覆盖越权和跨租户案例。
- 常规验证优先使用确定性离线夹具；外部模型服务验证单独记录，不把离线结果表述为真实服务验证。
- 检查失败后修复原因并重跑相关检查，不通过删除测试、降低阈值或排除安全案例掩盖失败。
- 完成修改后检查差异并运行 `git diff --check`；提交前运行相关测试、适用的格式与静态检查，并扫描待提交文件中的秘密和意外生成文件。纯文档修改进行相应的内容与格式检查即可。
- 对重要行为变化同步维护相关文档，记录设计依据、可复现命令、实际测试结果、指标变化和限制；未执行或未通过的验证必须如实说明。

## 当前数据隔离约束

- 用户当前只允许使用 `src/graphrag_prod/playground/static/industrial-demo-v1/` 循环水泵测试包。后续任务、测试、检索、问答、模型输入与推理依据均限定在这个包及用户据此新建的知识内。
- 其他工业语料及厂商 PDF 已移至项目外隔离区；未获用户明确恢复指令前，视为不存在，不读取、不检索、不导入、不用于模型提示或测试。历史文档、Git 历史和归档数据库中的对应内容同样不作为任务依据。
- 本地 `PLAYGROUND_PUMP_ONLY=1` 保持开启；不要启动旧 8000 服务或恢复旧示例库来绕过隔离。隔离清单只用于用户明确要求恢复时定位，不作为内容来源。
